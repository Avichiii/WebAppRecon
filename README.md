## Initialize Zap
```
zap.bat -daemon -port 8080 -config api.disablekey=true # Windows
./zap.sh -daemon -port 8080 -config api.disablekey=true # Linux
```

## Install & Run
```
git clone https://github.com/Avichiii/WebAppRecon
cd WebAppRecon
pip3 install -r requirements.txt
python3 app.py
```

